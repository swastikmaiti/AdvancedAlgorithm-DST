#ifndef GENERATE_INCOMING_GRAPH_H_INCLUDED
#define GENERATE_INCOMING_GRAPH_H_INCLUDED
#include "structures.h"

struct Graph* generate_incoming_graph2(struct Graph* graph);
struct Graph* generate_incoming_graph();

#endif // GENERATE_INCOMING_GRAPH_H_INCLUDED
